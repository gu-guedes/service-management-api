# ==================================
# PERFIL DEV — valores de conveniencia para rodar localmente
# Ativado por padrao (spring.profiles.active=${SPRING_PROFILES_ACTIVE:dev})
# ==================================
spring.datasource.url=jdbc:postgresql://localhost:5432/service_management_db
spring.datasource.username=postgres
spring.datasource.password=postgres

# dev-danger libera changesets marcados com context="dev-danger" (destrutivos).
# Nunca usar esse contexto no perfil prod.
spring.liquibase.contexts=development,dev-danger

jwt.secret=vWqR9bXk8N2pZ7fL4mS1vY6xC3hJ9gQ0dB5nH8tK2wA=

# usuario admin semente — criado automaticamente no startup se app_user estiver vazia
app.seed-admin.username=${SEED_ADMIN_USERNAME:admin}
app.seed-admin.password=${SEED_ADMIN_PASSWORD:changeme123}
